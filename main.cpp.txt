#include <stdio.h>

char *LireLettre(char *p_lettre)
{
    return p_lettre;
}

char *LireMot(FILE *monFichier)
{
   if (monFichier == NULL)
        perror ("Error opening file");
   else
   {
     char buffer [100];
     char *p_buffer = buffer;
     char tab_char[10];
     char lettre_char[1];
     char espace = ' ';
     //char *p_espace = espace;
     //tab_char = new char[10];
     const char *p_string = tab_char;
     const char *p_lettre = lettre_char;

     while ( ! feof (monFichier) )
     {
       //printf("coucou2\n");
       if ( fgets (p_buffer , 2 , monFichier) == NULL )
           break;
       //while (buffer!=espace)
       {
          // fscanf(monFichier, p_buffer, 0);
          printf("mm");
       }
       return p_buffer;
     }
   }
   return 0;
}

int main()
{
    FILE *monFichier;
    monFichier = fopen ("monFichier.txt" , "r");
    //printf("coucou\n");
    char *mot;

    do
    {
        mot = LireMot (monFichier);
        printf(mot);
        printf("\n");
    }
    while (mot != NULL);

    fclose (monFichier);

}


